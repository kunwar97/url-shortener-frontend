# URL Shortening Service Frontend (React)

## Installation Steps
- Run `yarn install` to install the dependencies.
- Change baseUrl in `.env` file.  
- Run `yarn start` to run react server.


## TODO:
- User Profile
- Edit URLs
- Graph in Logs
- UI improvements
